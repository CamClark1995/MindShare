<%= display_errors(@user) %>
<%= form_for @user do |f| %>
  <%= render 'flash' %>

  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit 'Create User' %>
  <%= link_to 'Cancel', login_path %>
<% end %>